<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnessfreaks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.allDoneActivity"></activity>
        <activity android:name=".Activities.excersicemain2Activity" />
        <activity android:name=".Activities.ExcersiceMain" />
        <activity android:name=".Activities.ShowPersonalExcersice" />
        <activity android:name=".Activities.WorkoutTypeActivity" />
        <activity android:name=".Activities.daysActivity" />
        <activity android:name=".Activities.myWorkout" />
        <activity android:name=".Activities.nutritionActivity" />
        <activity android:name=".Activities.ResetPasswordActivity" />
        <activity android:name=".Activities.MainActivity" />
        <activity
            android:name=".Activities.CheckEexcersiceActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.excersiceListActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.frontPageActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.RegistrationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.LauncherActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>